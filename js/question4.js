var ch=prompt("Enter a single character: ")
ch=ch.toLowerCase()

switch (ch) {
    case 'a' :
        case 'e':
            case 'i':
                case 'u':
                    console.log("Given character is vowel.")
                    break
                    default:
                    console.log("Given character is a consonent.")
}